"""
Bridge sub-package.

Mixins для TelegramBridge — каждый модуль группа методов по
ответственности. TelegramBridge остаётся фасадом, наследуясь от mixins;
поведение тождественно (это всё тот же класс, тот же self).
"""
